==============Content================
밑에 중복된 질문이 있었는데 약간 이해가 되지 않는 부분이 있어서 질문드립니다.

(2) 번에서
 file.h에 사용되어있는 원형 사용 -> ssufile에 관한 구조체 사용
 file구조체 초기화 및 파일 정보를 현재 프로세스 파일 디스크립터 테이블에 갱신 -> proc.h 를 참고하여 process구조체를 봤습니다

process구조체에 struct ssufile *file[NR_FILEDES]; 가 추가 되어있는데 NR_FILEDES 는 5값으로 define 되어있는것도 확인했습니다.

1. file_open함수에서 inode_open으로 받아온 inode의 값을 ssufile구조체에 갱신을 한 후 현재의
 process 구조체에 있는 파일디스크립터 테이블에 갱신을 하는 것으로 이해했습니다. 여기까진 맞나요?

2. 그렇다면 file_open함수에서 ssufile에 대한 지역변수 처리로 하여 매칭을 하게 될 경우 주소값에 대해서만 매칭이 되어 file_open함수가 끝났을때에는 inode,flags의 정보는 없어지게 되어서 밑에 질문게시판을 참고해 보니, 전역변수로 놓아도 된다는 말이겠죠?

3. 그리고 마지막으로 file[NR_FILEDES] NR_FILEDES의 의미가 궁금합니다. file_open을 했을때 저 파일디스크립터 테이블에 갱신시키는 부분에서 정해진 위치에 갱신을 시켜야 하는건가요..?
그럼 이에 따른 검사도 따로 로직으로 처리를 해줘야하나요..? 명세에는 나와있지 않아서 어떤 의미인지 궁금합니다.



==============Comment================
1, 2 네 맞는 것 같습니다..!
3 저는 nr_filedes 에 들어갈 인덱스를 전역변수로 선언해서 해당 인덱스를 증가시켜서 테이블에 갱신했습니다!

감사합니다..ㅎㅎ

해결했습니다

