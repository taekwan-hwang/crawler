==============Content================
https://oslab.ssu.ac.kr/main/index.php/oslab-main/2017-os/qa/?pageid=1&mod=document&keyword=fault&uid=1371
https://oslab.ssu.ac.kr/main/index.php/oslab-main/2017-os/qa/?uid=1371&mod=document&pageid=1

현재 위 링크들의 현상과 같은 현상이 발생중입니다.

만,  코드 점검을 해보니 실제 메모리로 맵핑해둔것도 맞는것 같고,

if문으로 값이 있는지 없는지 구별해서 넣는것 까지 해주는데.

테스트 해본 결과. '값이 안 들어갑니다.'

코드 실행전에 paging off를 해주고, 코드를 실행시키고, 다시 paging on을 시키는데

페이지에 넣어줘야 하는 값은 잘 생성되고,  

테이블 = (실제 주소값) 해서 넣어주는데

테이블에 들어있는 값을 확인해보라고 해서 확인해봤더니 값을 넣기 전, 넣고 난 뒤의 값이 초기값으로 동일합니다.

저 상황에서 추가적으로 체크할것이 있는지, 아니면 값이 왜 안들어가는지에 대한 힌트를 부탁드립니다...



==============Comment================
저도 같은 현상이 있었는데 가상주소랑 맵핑한게 정말로 아닌지 한번 확인해보세요 가상주소로 맵핑되어있는 값을 임의로 변경시 값이 변하지 않는 것 같습니다.

C0000000으로 시작하는 값이 실제주소인것이 맞지 않나요?

dirctory [인덱스] 의 값에서 present bit 확인하여 테이블이 존재하는지 아닌지 체크 해서 제어 하셨나요? 체크 해서 상황을 제어하셔야 합니다

테이블이 존재하는지는 확인했습니다. 상황제어라고 하면 테이블이 있으면 / 없으면의 처리를 다르게 하라는것이 맞나요?

명세를 다시 보시면 가상 메모리 주소 공간의 0~2MB까지는 실제 메모리 공간과 동일하게 맵핑되며 paging.c의 init_paging()함수에서 page table을 2MB까지 초기화합니다. init_paging() 함수 쪽을 다시 보시면서 pf_handler() 함수를 구현하시면 될 것 같습니다. 또한 RKERNEL_HEAP_START = 0x00200000이고 VKERNEL_HEAP_START = 0xC0000000입니다.

