==============Content================
현재 함수 호출후 리턴값을 SYSNUM의 위치에 저장을 구현중에 있습니다.  제가 생각하는 바로는 syscall handler 마지막에 *pt = ret을 해준 뒤에 
syscall.c의 매크로 함수에서 int 0x30다음에 pop 연산등을 이용해 구현을 하려는데 잘 되지 않고 있습니다. 

지금 가장 이해가 되지 않는것이 system call이 요청되었을때 관련 함수에서 리턴값이 어디에서 저장되고 어디에서 사용하는지 입니다. 혹시 관련해서 이해에 도움될만한 사이트나 정보가 있을까요???



==============Comment================
특별한 작업을 하시지 않는다면 syscall 함수에서 반환되는 리턴값은 맨위에 있는 int ret =0; 이게 반환되게 됩니다. 
맨밑의 ret;를 통해서 말이죠.

따로 디버거로 안돌려봐서 정확하게 어디서 이루어지는건지는 모르겠는데 별도 처리를 안해도 처음 스택포인터  기준 +4 위치에 저장된 값이 왠지 ret으로 넘어오더군요; 해당 위치를 수정하면 받아올 수 있을거 같긴 한데, 기본적으로  정확히 SYSNUM위치에 들어갔고 스택포인터가 움직이지 않았다면 pop 시 해당 위치에서 받아올 수 있습니다.

http://onecellboy.tistory.com/50 
저는 위의 링크를 참조하여 어셈블리 코드를 작성하였습니다. 
위 링크를 중간 이후의 내용을 보시면 어셈블리 코드에서 사용되는 레지스터와 메모리를 변수에 연결하여 input, output을 하는 코드가 여러가지 예시로 나와있으니 참고하시면 될 것 같습니다.

