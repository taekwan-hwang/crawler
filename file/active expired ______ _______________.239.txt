==============Content================
active가 empty 일때 expired가 가리키는 주소와 교환을 하려고 똑같은 임시 포인터를 만들어서 평소 변수 값 교환하는 방식으로 구현하였습니다.
그런데 교환후 sched_find_set을  한 result의 값에 NULL값이 들어갑니다. 제가 한 방식이 잘못된건가요???



==============Comment================
expired는 empty가 아닌가요?

제가 설명을 잘못드린것 같습니다! 음... priority array 2개를 가리키는 active와 exipred를 이용해서 교환을 하려구 하였습니다.
expired가 가리키는 배열에 active가 가리키던 배열의 값들을 이동시킨 후에 active가 가리키는 배열의 주소와 expired가 가리키는 배열의 주소를 바꿨습니다. 그래서 바뀐 후에도 active 포인터로 이용하려는데 리턴받은 값이 NULL값이 들어갑니다!

Active랑 expired 두 포인터만 서로 교환해주시면   되지 배열의 값을 굳이 옮길필요가 있나 싶네요  값을 이동 하고 포인터까지 바꾸어 버리면 아무 작업도 안한게 되지않을까요??

저도 같은 문제가 있었는데 expired배열에 리스트가 제대로 안들어갔더라구요.  혹시 active배열에 있는 리스트를 삭제하고 expired 배열에 넣는 건 제대로 들어갔는지 확인해보세요!

감사합니다!

