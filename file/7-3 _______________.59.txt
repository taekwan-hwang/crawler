==============Content================
전에 게시물에 init_inode_table에서 inode_table에 직접 넣는 것(?)과 inode_create 함수 쓰는 것의 차이 물어보는 질문이 있었는데 답변이 없어서 다시 올립니다..
무슨 차이인지 잘 모르겠습니다 ㅠㅠ



==============Comment================
해당 inode의 멤버변수를 제대로 설정하면 결과는 차이 없겠지만, 어차피 같은 기능을 mkdir같은 경우에도 쓸 건데 inode_create쓰는게 낫지 않을까요?

감사합니다!

말씀하신 두가지 모두 inode를 생성하고, 값을 변경시킬 수는 있겠지만, 과제의 범주에서 보지 않고 filesystem을 공부한다는 차원에서 보면, inode_create는 init_inodetable, make_dir말고도 일반파일을 생성할 때 함수라던가, 소켓, 등등 다양한 파일을 생성할 때 모두 사용될 것입니다. 그러한 상황을 고려한다면, inode_create는 함수로 분리되어있는것이 효율적이겠지요.

감사합니다!

