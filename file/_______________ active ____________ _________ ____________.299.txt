==============Content================
sched_find_set()함수에서 active배열에 가로부터 검사하면서 비어있지 않은 리스트가 있으면
get_next_proc()함수 인자로 넘겨줍니다.

이 프로세스를 실행하고 time_slice가 60이 되면 active함수에서 현재 프로세스를 지우고
expired에 현재 프로세스를 push_back하는데 문제는 이렇게 해도 

처음에 프로세스2가 선택되고 그 다음 프로세스도 2가 선택됩니다.
active배열에 아직 프로세스2가 남아서 2가 선택되는건지 다른 문제가 있는건지 모르겠습니다.

디버그 해보려고 active + cur_process->priority 이런식으로 접근해서 pid를 찍어봤는데 쓰레기값만 나옵니다.



==============Comment================
list_empty 을 이용해 해당 프로세스가 지워졌는지 확인해 보는건 어떠신가요?

list_init하셨나요?

init으로 해결했습니다 감사합니다.

