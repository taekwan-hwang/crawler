==============Content================
"현재 프로세스의 page directory에 인덱스를참고해서 해당 page table 항목에 page fault를 발생시킨 메모리 주소의 실제 메모리 주소를 추가" 하라는 명세가 이해가 안되는 상황입니다. 정확히는 주어진 명세 그림과 개념적으로 연결이 잘 안되는 상황입니다.

page directory와 page table은 주어진 함수를 이용하면 되는것 같은데, 이 결과값을 가지고 추출해야하는 실제 page fault 발생 메모리 추가 부분에서 막힙니다.

page directory와 page table에 page fault를 발생시킨 메모리 주소의 실제 메모리 주소를 추가하라 함은, 가상 메모리 상에서 발생한 fault 주소를 physical 주소로 변환하라는 뜻인가요?
어떤 접근으로 들어가야하나요?



==============Comment================
네. physical 주소로 변환하라는 뜻이에요.

cr0을 이용한 paging 상태 변경이 우선적으로 적용된 후에 물리주소를 변환해야하는 건가요?

memcpy_hard() 한 번 살펴보실래요?

옙 감사합니다!

