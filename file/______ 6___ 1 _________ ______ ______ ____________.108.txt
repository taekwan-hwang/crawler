==============Content================
disassemble 을 통해 특정 레지스터에 저장된 값이 리턴되는 것은 확인했습니다.
syscall 디파인 함수에서 int 명령어 이후에 복귀했을 때, syscall_handler 에서 pt 의 위치를 잘 설정하여 값을 넣어 줬다면 esp의 위치에도 맞는 값이 들어가 있을거라고 생각해서 pop 명령어를 통해 해당 레지스터에 값을 꺼내는 식으로 구현했는데요.

당연히 생각과는 다르게 동작합니다 ... 리턴되는 값이 쓰레기 값으로 리턴되어 콘솔에 이상한 문자가 잔뜩 찍히네요

이런 상황에서 몇가지 질문을 드리려고 합니다.

1. int 이후에 pop 명령어를 사용한다면 pop 전후에 esp에 대해 어떤 추가적인 조작이 있어야 하는 걸까요?
2. esp 값, 그리고 여타 레지스터 값들이 어떻게 변화하는지를 못 봐서 이런 질문을 하게 된 것이라서 ... 혹시 인라인 어셈블리를 스텝바이스텝으로 진행 할 수 있는 방법이 있을까요? 또 현재 상황에서 레지스터의 값을 보는 법도 궁금합니다.



==============Comment================
레지스터 값을 보는 법은 찾았습니다.. 구글링이 부족했네요.

info registers
info registers [레지스터이름]

1. 사실 pop을 사용하게 되면 esp도 같이 조작이 되므로 추가적인 조작은 안해도 될 것이라고 생각됩니다. 구현 방법은 일리가 있으나 pt의 위치를 설정함에 있어서 조금 문제가 있어서 동작이 제대로 되지 않는것으로 파악됩니다. 
2.에 대한 답은 댓글에 달으셨으니 생략하겠습니다.

방금 전 pop 만 사용하여 정상 동작하는 것 확인하였습니다.
인라인 어셈블리 진행 후 레지스터 값이 ret 에 할당되는 과정에서 문제가 있었습니다.
감사합니다.

