==============Content================
deallocated에서 계속 오류가 나서

do_malloc과 do_free에서 printk로 출력해보니

size와 blk_desc_cnt은 순서대로

256 0, 128 1, 104 2순으로 잘찍히는 거 같은데

do_free에서 st_addr은 계속 0값만 나오네요...

혹시 어떤게 문제일까요?



==============Comment================
학생들이 해결방법을 알려줄수 있을정도의 구현 방법 정도는 언급을 해주셔야 할 것 같습니다...

syscall.c의 void malloc에서 return syscall1(SYS_MALLOC, size); 이렇게 리턴을 하였습니다.
void free에서 printk mem_addr을 해도 0으로 나오네요...

저도 같은 부분에서 막혔었습니다, syscall.c의 define부분을 고치셔야할 것 같습니다. malloc이 return받는 과정에서 제대로 값이 전달되지 않으신거같습니다.

저는 do_malloc() 리턴값에 문제가 있었습니다.

아마 인터럽트 핸들러에서 pt에 리턴값을 저장할때,  인라인함수에서 pop을 할떄 등 어떤 문제로 인하여 do_free함수에 인자가 제대로 전달되지 않아서 생기는 문제일 겁니다.

값을 제대로 저장하였는지 확인해보세요. 다른쪽 문제였지만 저도 비슷하게 값이 계속 0으로 나와 확인해보니 값을 넣지 않았던 부분이 있었습니다.

감사합니다. return값 오류였습니다. 해결했스빈다.

