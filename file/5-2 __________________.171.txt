==============Content================
자식 디렉토리 테이블과 페이지 테이블에 원하는 값을 잘 넣어준것을 확인했고 
pd_copy()함수 수행 이후에 pd값을 VH_TO_RH(pd)매크로를 사용해서 실제 주소로 리턴 해줬습니다. 
그런데 첨부한 스크린샷의 부분에서 수행을 하지 못하는데.. 어디가 잘 못 된건지 알 수 있을까요..



==============Comment================
이 스크린샷만으로 어떤 부분에서 에러가 난 지 정확히 모르겠지만 아마도 cr3에 잘못된 값을 설정하는 것 같습니다. p 명령어 등으로 어떤 값이 저장되는지 확인해보세요.

p명령어로 찾다가 잘못된 부분을 발견했습니다 ! 
감사합니다 ~

명세에 따라 page directory와 page table의 내용을 복사했어야 했는데 directory table에 엉뚱한 값을 넣어줘서 발생한 문제였습니다 ~

