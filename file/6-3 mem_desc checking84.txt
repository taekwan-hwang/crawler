==============Content================
3번을 구현 중입니다.

memory deallocated size의 값이 free연산을 한 번 하면 해당 크기만큼 삭제를 해줘야는데, 값이 그대로 이어지고 있는 상태입니다.
해당 blk_list 구조체 변수의 값은 새로 초기화해주었습니다.

deallocated size, address가 출력된 후 mem_desc checking 연산을 아직 해주지 않아서 생기는 문제인것도 같은데...
이건 어떤 이유로 해주는 건가요..?



==============Comment================
값이 그대로 이어진다는 것을 제가 제대로 이해한 것이 맞다면 free()를 이어서 수행해주는데 같은 값이 출력된다는 의미같은데, 
이 경우 생각해볼 수 있는 것이라면 인자로 받은 stat_addr가 같은 값일 수 있다는 생각이 듭니다.
인자로 받게되는 stat_addr는 do_malloc의 리턴값을 받아오는 것이니 그 부분에서 문제가 있는지 확인해보는 것은 어떨까요?

감사합니다 제출 전까지 확인해보겠습니다.

