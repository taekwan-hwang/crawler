==============Content================
CASE1을 모두 수행한 후에 page fault가 뜹니다.
(사진처럼)

만약 모든 프로세스를 다 종료할경우, 특정 변수를 false로 만들어서

schedule 내부에 함수가 아무것도 실행되지 않도록 했습니다.

printk로 찍어보면 schedule 함수가 아무동작 없이 수십차례 동작하다가 갑자기 page fualt가 뜨는데 원인을 알 수 있을까요>



==============Comment================
이미 수행이 끝난 프로세스로 context switching하고 있는지, 아니면 idle() 함수가 어떤 동작을 취하고 있는지를 확인해보십시오.

idle내에서는 while(1) { shchdule() }; 만을 선언하고 있습니다..
배열이 모두 끝난 경우 schedule 함수에서 swithcing 코드 자체를 실행하지 않습니다. (printk로 확인했습니다.)

배열이 모두 끝난 후에도 0번 프로세스<->0번 프로세스로 switching 동작을 해줘야 하나요?

switching 작업을 해주더라도 0번 프로세스를 종료하지 않는 이상 page fault가 발생하지는 않을 것 같습니다.
질문자님의 답변을 확인해보면 그래도 의심이 가는 함수가 schedule() 함수인 것 같습니다.

저 같은경우 수행이 다 끝났을때는 schedule함수에서 수행이 다끝났을때에 대한 flag 변수를 전역으로 선언한 뒤에 return ; 해주는 방식으로 해결했었습니다.

