==============Content================
8-4,5에 있어서 우선

passwd파일을 통한 로그인을 위해 do_write함수를 완성했습니다.
passwd파일에는 ssuos:oslab가 쓰여져있는 상태에서

ssufile구조체에 있는 pos는 write를 했으므로 11로 바뀌어있을것인데

로그인을 위해 passwd를 read해올때는 id를 읽을때 처음부터 ssuos까지 읽으므로 pos = 5
password를 읽을때 그 이후로 부터 읽어온 후 pos = 11 

이런식의 전개가 되려면 pos는 read, write 별도로 존재해야하는것이 아닌가요ㅠㅠㅠ

힌트좀 주시면 감사하겠습니다 ㅠㅠ



==============Comment================
아예전체를읽어서스트링토큰함수로나눠도되지않을까요?

그런데 write를 한 입장에서 ssuos:oslab인 상태에서는 그 passwd파일의 pos의 값은 11로 저장되어있을텐데 명세에서는 쓰거나 읽을때 pos를 그 위치로 변수를 증가시키라고 나와있어서 그렇게 생각했는데요..
passwd를 검사후 로그인하기위해 read를 쓸때도 pos값으로 접근해서 그 위치부터 읽어오면 당연히 안되고 처음부터 읽어온후 스트링토큰해서 체크는 할수는 있을것 같은데 그럼 명세랑 뭔가 좀 안맞는게 아니에요ㅠㅠ?

조교님께 메일 드렸는데 상관없다고 하셨습니다!

감사합니다!ㅎㅎㅎ

strtok_r 쓰신건가요?

string.h 헤더파일에 들어가셔서 함수 설명 보시면 될 것 같아요!

write를 진행한 후에 다시 open해서 새로 fd를 생성 뒤 읽어드리게 구현해도 될 것 같습니다.

전 file.h 에 file_seek() 원형이 있길래 그걸로 pos값을 원하는 위치에 세팅한 후 read 했습니다.

