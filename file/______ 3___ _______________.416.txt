==============Content================
과제 명세에서 Keyboard Handler에서 콘솔 출력 함수를 직접 호출하지 않게 할 것 이라는 말이 기본적으로 구현되어있는 소스코드에서 
kbd_handler의 printk를 주석처리하고 새롭게 구현하라는 뜻인가요..?



==============Comment================
정확하지는 않지만 저도 일단 그렇게 했네요 ㅠ

네ㅠㅠ감사합니다

키보드 핸들러에서 출력함수를 호출하면 안됩니다. kbd_handler() 안에서 화면에 직접 출력하는 과정을 수행하면 안된다는 뜻입니다.새롭게 구현한 함수이던 printk 함수이던  상관없습니다.

PrintChar이나 PrintCharToScreen과 같은 함수도 직접적으로 사용해선 안된다는 말일까요..?

kbd_handler안에서는 사용하시면 안됩니다.

