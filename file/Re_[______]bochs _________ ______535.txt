==============Content================
과제 제출일부터 지금까지 계속 붙잡으면서 잘 안되는 부분이 있어서 질문 올립니다.
현재 저는 bootprint.asm 수정 저장 후에 터미널 상에서 nasm bootprint.asm -f bin -o bootprint.bin 으로 아웃풋 뽑고서 src 파일경로에서 make run을 하고 있는데요,

이틀 동안 과제를 하면서 수없이 코드를 변경했는데, 단 한번도 에뮬레이터 상에서 변화가 있던 적이 없었습니다.
처음에는 asm 내부 코드 상의 문제라고 생각했는데, 참고용 서적이나 웹 상에 정상적인 코드를 입력해도 아무런 반응이 없어서 제가 잘못하고 있다는 느낌이 드는데요,
제가 하고 있는 방법에 어떤 점이 문제인지 궁금합니다.



==============Comment================
아 해결했습니다. 빌드 과정에서 생략한 부분이 있었습니다.

어떤 과정이 생략되었는지 혹시 알 수 있을까요?

make명령어를 생략하지 않았을까요

제가 글에서 쓴 nasm ~ 라인은 필요없더라구요

네 make 해주면 알아서 처리해주더군요

