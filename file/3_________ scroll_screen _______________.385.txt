==============Content================
#define으로 되어있는 부분을 꼭 해제해서 이용하여 스크롤을 구현해야하는지 아니면 다른방법으로도 구현만 하면 되는지 궁금합니다



==============Comment================
개인적인 생각으로는 #define에 이미 구현되 있는 함수가 몇개 있다보니 사용하면 더 빠르게 구현 하실 수 있을것 것 같고 다른 방식이 있다면 다른 방식으로 풀어도 되지 않을까요?..(명세서 상에서는 큰 문제는 없을 것 같습니다.)

그 부분에 있는 함수중 하나를 썼는데 그 함수를 적당한 위치해 넣으니 스크롤하는데 큰 도움이 됐습니다. 원래 위쪽SSUOS로고가 안나왔는데 위에서부터 차례로 출력이 됬습니다.

두 분 감사합니다 ~~~

해제하지 않고 구현이 가능하시다면 하셔도 상관 없습니다.

