==============Content================
malloc()안에서 return syscall1(SYS_MALLOC,size);을 해줘서 do_malloc이 호출되면 do_malloc() 안에 있는 

printk("Allocating size... : %d\n", size); 에서 size 값은 정상적으로 출력되야하는거 아닌가요.. 근데 계속 21아니면 22만 출력되는데 

문제가 뭔지 모르겠네요. 왜 매개변수를 제대로 전달받지 못하는 걸까요...?



==============Comment================
저도 비슷한문제가있는거같은데 , 문제가있을곳은 syshandler 부분일거같습니다

저는 비슷한 문제에서 자료형을 바꾸었더니 해결되었습니다.

시스템 콜 핸들러 부분에서 범용 레지스터의 값을 찍어 보면 2, 10, 22 등의 값들을 확인하실 수 있습니다.
인라인 어셈에서 매개변수를 할당한 레지스터와 실제로 전달된 매개변수를 arg0 등에 담는 대상이 동일한지 확인 하시는 것이 좋을 듯 합니다!

