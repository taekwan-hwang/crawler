==============Content================
msg db "Hello"에서  mov al, [msg]를 쓰면 al에 "H"가 저장되는거 아닌가요?
mov byte[msg],"H"를 쓴 후 mov al,[msg]를 사용하면 "H"가 출력되지만 mov al,[msg]만 사용하면 이상한 값이 뜹니다.



==============Comment================
offset만 쓰면 세그먼트 레지스터 값에 따라 엉뚱한 곳을 가리킬 수 있어요.

과제 1관련 질문 위에서 하나씩 봐 보시면 적으신 내용 중에 뭔가 빠진 부분을 확인하실 수 있을거에요

