==============Content================
과제에 "do_read 실행시 file구조체의 pos 변수를 증가시켜야함"이라고 명시되어있는데 cat을 실행하여 길이가 11인 passwd 파일을 읽을 경우 passwd파일의 ssufile 구조체의 pos값이 0, 11, 20중 어떤 값이 들어가야 맞는지 헷갈립니다.



==============Comment================
11을 읽었으니 11이 맞지않을까요. 인자 len 만큼 pos를 증가시켜서...

그럼 cat passwd를 두 번 실행하면 두 번째에는 아무것도 출력이 안되어야 하나요?

명세에는 'pos 변수는 read, write 함수가 다음번에 읽거나 쓸 위치를 가리키는 변수여야함 (0으로 초기화)' 이렇게 쓰여있는데 뒤에 0으로 초기화라는 말을 read할 때 0으로 초기화하여 처음부터 읽는 것으로 해석하여 read시에 pos를 0으로 초기화하고 읽어오고 나서 len값만큼 pos값을 증가시키는 것으로 구현하였습니다.
pos변수를 직접 활용하려면 file_seek()등의 함수가 구현되어야 할 것 같고 이 부분은 구현사항이 아닌 것 같습니다

cat은 fork를 통해 새로운 프로세스를 만들어서 실행하기 때문에 두번 수행해도 항상 pos가 0으로 초기화되어있는 상태에서 실행됩니다.

명세에 ' read, write 함수는 실제로 읽거나 쓴 글자수만큼 file구조체의 pos 변수를 증가시켜야함 ' 이라는 항목이 있으므로, 실제로 읽은 길이인 11이 되는것이 맞습니다.

