==============Content================
process 구조체에 struct ssufile *file[NR_FILEDES] 이 존재하는것은 확인했습니다.
포인터로 구현되어 있다보니 파일오픈 시 메모리 영역을 할당해서 주소값을 저장하거나
이미 확보된 메모리의 주소값을 저장해야 할 것으로 보이는데요

기존 4번, 7번 과제 등 에는 전역으로 선언된 배열이 있어 포인터로 할당이 가능했었습니다만,
이번에는 어떻게 해야 할지 감이 잘 잡히지 않네요 ...

어떤 방식으로 해결 해야 하나요? ....ㅠㅠ



==============Comment================
전역변수도 NR_FILEDES 크기 배열로 만들어서 하셨다는 말씀인가요??

프로세스 안에는 ssufile구조체의 포인터배열이 들어가 있는 상태입니다. 어떤 할당도 되어있지 않은 상태로 말씀하신대로 파일 오픈 시 포인터 배열에 ssufile 구조체를 할당해주시면 됩니다.

함수블록내에서 새로 선언하여 할당하라는 말씀이신가여?
그럼 함수블록 나가서 다른 함수가 중간에 호출되면 스택영역에서 덮어씌워지지않나요...?

malloc이 없는 상태이기 때문에 명세에 제약사항이 없어서 저의 경우에는 페이지를 하나 할당한 전역변수를 하나 만들어서 heap영역처럼 사용해서 구현했습니다.

