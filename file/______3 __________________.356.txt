==============Content================
아직도 문자열출력 감을 못잡아서 질문드리는데
키보드 핸들러에서 printk함수를 지우고 핸들러함수내부에서 printk함수기능을 구현해야 하는것인지
아니면 다른곳을 수정을 하는것인지 감을 못잡겠습니다



==============Comment================
키보드 핸들러에서 printk함수를 지우고 핸들러함수내부에서 printk함수기능을 구현해야 하는것인지
아니면 다른곳을 수정 => 둘 다 명세서에서 요구하는 조건에만 부합하면 되므로 가능할 것 같은데 다른곳을 수정하는게 좀더 쉬워보입니다

kbd_handler에서만 printk 등 콘솔 출력함수를 사용하지 마세요.

printk함수는 다른 모듈에서도 많이 사용되는 부분이므로 이를 지우면 많이 복잡할 것 같습니다. 해당 kbd_handler함수에서만 printk를 사용하지 않으면 되는 것으로 알고있습니다.

kbd_handler에서만 printk 등 콘솔 출력함수를 사용하지 마세요.

kbd_handler 함수에서 키보드 인터럽트를 처리하는 도중에 출력함수를 사용하지 말라는 의도인 것 같습니다.
kbd_handler에서 직접적인 출력함수 호출만 없으면 사용해도 괞찮을 것 같습니다

예

저는 kbd_handler에서 문자 데이터를 저장하는 역할만 부여했고, 다른 부분에서 문자 출력을 수행했습니다..!

네 그렇게 하는게 맞습니다.

